# Epi With Python

這是一個以 **繁體中文科普解說** 為主、涵蓋 **流行病學統計、機器學習、深度學習** 的 Python 學習網站。

## 你將獲得

- 傳染病流病分析完整主線
- 每章可直接複製執行的程式碼
- 使用 `uv` 統一管理環境、執行、建置

## 一行開始

```bash
uv sync && uv run jupyter lab
```

## 核心約定

- 所有安裝與執行命令都用 `uv`
- 所有範例優先可讀、可跑、可解釋
- 每章都包含限制與誤用提醒
